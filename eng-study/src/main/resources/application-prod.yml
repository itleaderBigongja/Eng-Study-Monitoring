spring:
  config:
    activate:
      on-profile: prod

  application:
    name: eng-study

  threads:
    virtual:
      enabled: true

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres-service.eng-study.svc.cluster.local:5432/DEV_DB}
    username: ${SPRING_DATASOURCE_USERNAME:rnbsoft}
    password: ${SPRING_DATASOURCE_PASSWORD:rnbsoft}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 20
      minimum-idle: 10
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  devtools:
    restart:
      enabled: false

  jackson:
    time-zone: Asia/Seoul

# JWT 설정
jwt:
  secret: ${JWT_SECRET:local-jwt-secret-key-for-kubernetes-development-only}
  access-token-expiration: 3600000
  refresh-token-expiration: 604800000

# CORS 설정
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:30080,http://nginx-service}
  allowed-methods: GET,POST,PUT,DELETE,PATCH,OPTIONS
  allowed-headers: "*"

# ========================================
# ✅ MyBatis 설정
# ========================================
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.eng.study.engstudy
  configuration:
    map-underscore-to-camel-case: true
    jdbc-type-for-null: 'NULL'
    cache-enabled: false
    default-fetch-size: 100
    default-statement-timeout: 30
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  shutdown: graceful

# ========================================
# ✅ 로깅 설정
# ========================================
logging:
  file:
    name: ${LOGGING_FILE_NAME:/logs/application.log}
    path: /logs

  level:
    root: INFO
    com.eng.study.engstudy: INFO
    org.springframework.web: INFO

    # ==========================================
    # [중요] 보안 로그 다이어트 (INFO로 변경)
    # ==========================================
    org.springframework.security: INFO                    # 헬스 체크할 때마다 로그 폭탄이 떨어지는 것을 방지합니다.
    org.springframework.security.web: INFO                # 웹 요청에 대한 보안 필터(URL 접근 권한 등) 동작을 봅니다
    org.springframework.security.authentication: DEBUG    # 인증 시도, 성공/실패 처리 과정을 봅니다


    # ==========================================
    # [중요] SQL 로그 최적화
    # ==========================================
    # SQL이 정말 궁금할 때만 아래 'mapper'를 DEBUG로 바꾸세요.
    # 평소에는 INFO로 둬야 성능이 빨라집니다.
    com.eng.study.engstudy.mapper: DEBUG
    com.eng.study.engstudy.mapper.SqlLog: INFO
    org.apache.ibatis: INFO
    org.springframework.jdbc.core: INFO
    org.springframework.jdbc.core.JdbcTemplate: INFO

    # ✅ DB 예외 로깅
    org.springframework.dao: ERROR
    org.springframework.jdbc.support: ERROR
    org.mybatis.spring: ERROR

    # 커스텀 로거
    com.eng.study.access: INFO
    com.eng.study.audit: INFO
    PERFORMANCE_LOGGER: INFO

    # (선택) 만약 Java 코드에서 "SecurityLog"라는 이름으로 로거를 따로 쓰신다면 추가
    SecurityLog: INFO

  pattern:
    # (팁) 불필요하게 긴 패키지명을 줄여서 로그 가독성을 높였습니다 (%logger{36} -> %logger{10})
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{10} - %msg%n"

# Actuator 설정
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  prometheus:
    metrics:
      export:
        enabled: true